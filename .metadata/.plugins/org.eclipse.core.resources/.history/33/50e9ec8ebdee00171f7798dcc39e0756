package third;

import java.awt.Graphics;
import java.awt.Panel;

public class mypanel extends Panel {
  
	public void paint(Graphics g)
	{
		for(int i = 0;i<10;i++)
		{
			g.drawString("*",(int)(Math.random()*300),(int)(Math.random()*400));
		}
	}
}
